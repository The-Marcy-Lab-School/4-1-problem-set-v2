// first add the properties and methods to this object directly
const cellphone = { };

// Then make a factory function that can return dynamic cellphone objects
const cellphoneFactory = () => ({

});

module.exports = {
  cellphone,
  cellphoneFactory,
};
